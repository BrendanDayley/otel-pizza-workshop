# Dash0 Configuration
# Get your token from: https://app.dash0.com → Settings → Auth Tokens

DASH0_AUTH_TOKEN=your_token_here

# Your Dash0 region endpoint
# Check your Dash0 URL to determine your region:
# - US West: https://ingress.us-west-2.aws.dash0.com:4317
# - EU West: https://ingress.eu-west-1.aws.dash0.com:4317
DASH0_ENDPOINT=https://ingress.us-west-2.aws.dash0.com:4317

# Which dataset the three services ship into, and the environment name they are
# tagged with. Both are optional.
DASH0_DATASET=default
DASH0_ENVIRONMENT=workshop

# Transport for the three services. `grpc` matches the :4317 endpoint above.
# If your network blocks port 4317, set this to `http/protobuf` and drop the
# `:4317` from DASH0_ENDPOINT.
DASH0_OTLP_PROTOCOL=grpc

# Optional: browser monitoring for the frontend
#
# Off unless BOTH variables below are set. This token is served to browsers as
# part of the page, so create a SEPARATE auth token for it, scoped to Ingesting
# only and to this dataset only — never reuse DASH0_AUTH_TOKEN here.
#
# Use the OTLP-over-HTTP endpoint (no :4317 port), from
# https://app.dash0.com/settings/endpoints:
# - US West: https://ingress.us-west-2.aws.dash0.com
# - EU West: https://ingress.eu-west-1.aws.dash0.com
DASH0_WEB_ENDPOINT=
DASH0_WEB_AUTH_TOKEN=

# Optional: turn all instrumentation off without editing anything else
# OTEL_SDK_DISABLED=true

# Optional: Enable debugging scenarios
SLOW_KITCHEN=false
NO_DRIVERS=false
ERROR_RATE=0
